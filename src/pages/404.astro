---
import Base from '../layouts/Base.astro';
---
<Base title="Not Found" back={{ href: '/', label: 'Notes' }}>
  <h1 class="large">Note not found</h1>
  <p class="empty">This note doesn’t exist, or it moved.</p>
</Base>
