{
  "name": "comments-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "db:local": "wrangler d1 execute blog-comments --local --file=schema.sql",
    "db:remote": "wrangler d1 execute blog-comments --remote --file=schema.sql",
    "deploy": "wrangler deploy"
  },
  "devDependencies": { "wrangler": "^4" }
}
